name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - nbval
  - coverage

  - pyyaml
  - numpy
  - matplotlib
  - pandas
  - rdkit
  - networkx
  - pint
  - scipy
  - openforcefield
  
# Pip-only installs
  #- pip:
  #  - codecov
  #   - pyaml
