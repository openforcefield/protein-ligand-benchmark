name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - yaml
  - numpy
  - simtk
  - urllib
  - json
  
    # Pip-only installs
  - pip:
  #  - codecov
     - pyaml
