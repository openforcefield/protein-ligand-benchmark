name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - yaml
  - numpy
  - openmm
  - pandas
  
    # Pip-only installs
  - pip:
  #  - codecov
     - pyaml
