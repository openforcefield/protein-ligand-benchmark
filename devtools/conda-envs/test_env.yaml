name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - nbval
  - coverage

  - pyyaml
  - numpy
  - matplotlib
  - pandas
  - rdkit
  - networkx
  - conda-forge::pint
  - scipy
  - openforcefield
  - requests
  
# Pip-only installs
  #- pip:
  #  - codecov
  #   - pyaml
