name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - nbval
  - coverage < 5.0

  - pyyaml
  - numpy
  - matplotlib
  - pandas
  - importlib_resources
  - rdkit
  - networkx
  - pint
  
  # Pip-only installs
  #- pip:
  #  - codecov
  #   - pyaml
