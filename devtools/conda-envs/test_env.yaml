name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - pyyaml
  - numpy
  - matplotlib
  - openmm
  - pandas
  - importlib_resources
  - rdkit
  - networkx
  
  # Pip-only installs
  #- pip:
  #  - codecov
  #   - pyaml
