name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  - nbval
  - coverage < 5.0

  - pyyaml
  - numpy
  - matplotlib
  - pandas
  - rdkit
  - networkx
  - pint
  - scipy
  - openforcefield
  - importlib_resources
  
# Pip-only installs
  #- pip:
  #  - codecov
  #   - pyaml
