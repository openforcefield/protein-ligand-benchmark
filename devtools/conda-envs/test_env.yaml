name: test
channels:
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - pyyaml
  - numpy
  - openmm
  - pandas
  - importlib_resources
  
    # Pip-only installs
  #- pip:
  #  - codecov
  #   - pyaml
